# Generated by the harness pipeline — regenerate via harness/phase1/ quickstart.
# These are derived artifacts, never source of truth. Real gold is rebuilt from blind labels.
harness/phase1/candidates_pool.json
harness/phase1/labels_*.json
harness/phase1/gold.json
__pycache__/
*.pyc
.DS_Store
.claude/

# Kept on disk, out of the published spine (see REPO_STATE.md).
# v1.1/ is a stale full-repo snapshot; gem_gpt_test/ is scratch. Remove these
# lines and `git add` them if you want either in history.
v1.1/
gem_gpt_test/

# --- pre-push quarantine (added during cleanup) ---
# Session scratch: zips, working drafts, the TRIBE PDF. Salvage keepers, do not bulk-add.
"XOP WEIGHTS 3.2/"
XOP WEIGHTS 3.2/
# Out-of-spine material kept on disk but never published (consciousness/witness construct, etc.)
_private/
